CREATE TABLE user (
    id BIGINT,
    img VARCHAR(255),
    name VARCHAR(255),
    playtime FLOAT,
    short_description TEXT,
    website VARCHAR(255),
    recommended TEXT,
    minimum TEXT,
    link VARCHAR(255),
    file_date DATE
);
